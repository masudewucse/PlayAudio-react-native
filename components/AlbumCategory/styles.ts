import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 10,
    },
    title: {
        color: 'black',
        fontSize: 24,
        fontWeight: 'bold',
    },

});

export default styles;